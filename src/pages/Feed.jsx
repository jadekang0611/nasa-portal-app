// This is where a user sees photos from the 'Mars Rover' endpoint
// A signed in user can not only view the photo feed, but also pin (a.k.a save) photo(s)
// he or she likes
// A unsigned-in user can only view the photo feed.
import React from "react";
import Layout from '../components/shared/Layout';


const FeedPage = () => {
  

  return <Layout title="Feed">Feed Page</Layout>;
}

export default FeedPage;